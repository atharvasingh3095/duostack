# Global Decisions

## Format
[Date] | [Decision] | [Reason]

## Log
{{DATE}} | DuoStack v{{DUOSTACK_VERSION}} initialized
{{DATE}} | Each project = own GitHub repo | Clean separation
{{DATE}} | Platform files local only | Privacy
{{DATE}} | Git CLI via terminal MCP | No token needed
{{DATE}} | DuckDuckGo MCP for search | Free, no API key